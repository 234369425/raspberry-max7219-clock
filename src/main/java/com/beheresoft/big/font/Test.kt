package com.beheresoft.big.font

import com.beheresoft.raspberryPi.font.FontModel

fun main() {
/**
    System.setProperty("font.location", "I:\\clock\\fonts")
    val b = EnglishString().getEnglishBuffer("abcdefghijklmn")
    val c = FontModel.parse("abcdefghijklmn")

    if (b.size != c.size) {
        println("error")
        return
    }

    for (i in b.indices) {
        if (b[i] != c[i]) {
            println("error $i ${b[i]} ${c[i]}")
            return
        }
    }

    println("ok")*/

}